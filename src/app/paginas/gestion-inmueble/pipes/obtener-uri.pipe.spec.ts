import { ObtenerUriPipe } from './obtener-uri.pipe';

describe('ObtenerUriPipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerUriPipe();
    expect(pipe).toBeTruthy();
  });
});
